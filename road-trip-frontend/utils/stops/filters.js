import stopData from "./stops.json";

export const levels = {
    Relaxed: 0,
    Moderate: 1,
    High: 2,
    Extreme: 3,
};

export const levelOptions = ["Relaxed", "Moderate", "High", "Extreme"];
export const filters = stopData;
